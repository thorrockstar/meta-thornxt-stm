--- a/drivers/mtd/mtdblock.c	2025-11-02 14:14:42.000000000 +0100
+++ b/drivers/mtd/mtdblock.c	2026-04-18 00:24:23.710753002 +0200
@@ -261,10 +261,6 @@
 		return 0;
 	}
 
-	if (mtd_type_is_nand(mbd->mtd))
-		pr_warn_ratelimited("%s: MTD device '%s' is NAND, please consider using UBI block devices instead.\n",
-			mbd->tr->name, mbd->mtd->name);
-
 	/* OK, it's not open. Create cache info for it */
 	mtdblk->count = 1;
 	mutex_init(&mtdblk->cache_mutex);
